import type BattleState from "@common/models/battle_state";

const getFirstInRows = (battleState: BattleState) => {
  const firstInRows: [number, number][] = [];

  return firstInRows;
};

export default getFirstInRows;